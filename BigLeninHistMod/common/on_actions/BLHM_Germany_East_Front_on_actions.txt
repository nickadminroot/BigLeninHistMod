on_actions = {
	on_war_relation_added = {
		effect = {
			if = {
				limit = {
					ROOT = { tag = GER }
					FROM = { tag = SOV }
					NOT = { ROOT = { has_country_flag = GER_east_front_war_with_ussr_started } }
				}
				ROOT = { country_event = { id = blhm_germany_east_front.1 hours = 1 } }
			}
			if = {
				limit = {
					ROOT = { tag = SOV }
					FROM = { tag = GER }
					NOT = { FROM = { has_country_flag = GER_east_front_war_with_ussr_started } }
				}
				FROM = { country_event = { id = blhm_germany_east_front.1 hours = 1 } }
			}
		}
	}
}
