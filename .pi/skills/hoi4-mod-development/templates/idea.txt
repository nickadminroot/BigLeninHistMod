# Idea / National Spirit Template
# Copy this block into your idea file in common/ideas/TAG.txt

ideas = {
    country = {                         # Visible national spirits
        TAG_my_spirit = {
            allowed = { always = no }            # Not purchasable from menu
            allowed_civil_war = { always = yes } # Survives civil war
            removal_cost = -1                    # Cannot be manually removed
            picture = generic_production_bonus   # Icon sprite name

            # Country modifiers:
            modifier = {
                industrial_capacity_factory = 0.05
                production_speed_industrial_complex_factor = 0.1
                research_speed_factor = 0.03
                army_org_factor = 0.1
                conscription = 0.01
            }

            # Equipment bonuses (uncomment if needed):
            # equipment_bonus = {
            #     infantry_equipment = {
            #         build_cost_ic = -0.10
            #         instant = yes
            #     }
            #     armor = {
            #         armor_value = 0.1
            #         reliability = 0.05
            #     }
            # }
        }
    }

    hidden_ideas = {                    # Not visible, effects only
        TAG_hidden_modifier = {
            modifier = {
                # Hidden effects here
            }
        }
    }
}
