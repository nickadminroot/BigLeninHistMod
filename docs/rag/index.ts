/**
 * Legacy Pi extension entry point.
 *
 * Documentation search is now implemented by the standalone
 * `scripts/docs-search.mjs` CLI. This file is intentionally inert so old
 * imports do not register a second Pi-only search tool.
 */
export default function legacyDocsSearchExtensionRemoved() {
  // Intentionally empty.
}
